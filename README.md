# hw_sqflite — تطبيق مهام باستخدام Flutter و SQLite ✅

**ملخص المشروع**
---
تطبيق **مهام (To‑Do)** مبني باستخدام **Flutter** ويستخدم **SQLite** عبر حزمة `sqflite` لتخزين المهام محليًا على الجهاز. يسمح التطبيق بإنشاء مهام تحتوي على: **عنوان، وصف، تاريخ استحقاق، أولوية، وفئة (category)**، مع إمكانيات التصفية والبحث وتعديل/حذف المهام.

---

## ✨ الميزات
- إضافة مهمة جديدة مع الحقول: العنوان، الوصف، تاريخ الاستحقاق، الأولوية، الفئة
- عرض قائمة المهام مع دعم الفلترة بحسب الفئة والأولوية
- تعديل وحذف المهام
- تخزين محلي مستدام باستخدام SQLite (`sqflite`)
- واجهة بسيطة قابلة للتوسيع

## 📁 بنية المشروع (مختصر)
- `lib/main.dart` — نقطة الدخول للتطبيق
- `lib/controllers/task_controller.dart` — منطق إدارة الحالة والعمليات على المهام
- `lib/db/db_helper.dart` — طبقة الوصول إلى قاعدة البيانات (SQLite)
- `lib/models/task_model.dart` — موديل بيانات المهمة
- `lib/views/` — شاشات التطبيق (`home_screen.dart`, `add_task_screen.dart`, `task_detail_screen.dart`)
- `lib/widgets/` — مكوّنات قابلة لإعادة الاستخدام (`task_tile.dart`, `input_field.dart`)
- `android/`, `ios/`, `web/` — إعدادات بناء النظام الأساسي

> ملاحظة: هذا المشروع يستخدم حزمة `sqflite` التي تعمل على منصات الهاتف (Android/iOS). على الويب، يلزم استخدام حلول بديلة أو استخدام التخزين المشروط.

## 🛠️ المتطلبات (Requirements)
- Flutter SDK (محدث)
- Android SDK و/أو Xcode (لإنشاء تطبيقات للأجهزة)
- اتصال إنترنت لتحميل الحزم

## 🚀 طريقة التشغيل (Local development)
1. استنخِب المشروع:

```bash
cd hw_sqflite
flutter pub get
```

2. لتشغيل التطبيق على جهاز متصل أو محاكي:

```bash
flutter run
```

3. لبناء ملف APK للإنتاج (Android):

```bash
flutter build apk --release
```

4. لتحليل الأكواد واكتشاف التحذيرات:

```bash
flutter analyze
```

5. لتشغيل الاختبارات (إن وجدت):

```bash
flutter test
```
اسم الطالب :زكرياء علي
